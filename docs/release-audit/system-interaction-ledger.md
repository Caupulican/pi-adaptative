# System Interaction Audit Ledger

> Cross-system audit log. A component-level green test is not closure. Current source and deterministic
> reproductions outrank this summary when they disagree.

## Method

Every edge is reviewed in this order:

1. **Structure** — which producer, owner, consumer, and cleanup path are connected?
2. **State** — which identity and transition are authoritative now?
3. **Time** — what can change while a callback, provider request, process, timer, or cleanup is pending?
4. **Failure** — what happens if that change occurs at the worst possible boundary?

Static scans, logs, and Jev judgments generate candidates. A defect is confirmed only by a deterministic
reproduction with a negative control. A repair closes only after the focused regression and adjacent tests pass.

Status vocabulary:

- `FIXED/PUSHED`: confirmed, repaired, tested, committed, and pushed.
- `FIXED/STAGED`: confirmed, repaired, and tested in the current worktree; not yet committed.
- `FIXED/WORKTREE`: confirmed, repaired, and tested in the current worktree; not yet staged.
- `CONFIRMED`: deterministic reproduction exists; repair is not complete.
- `CANDIDATE`: source or Jev indicates risk; deterministic reproduction is pending.
- `REJECTED`: the candidate is unreachable or an existing owner/guard preserves the invariant.
- `INCOMPLETE`: required evidence could not be obtained.

## Findings

| ID | Boundary | Structure | State | Time | Worst-moment failure | Evidence | Status |
|---|---|---|---|---|---|---|---|
| SI-001 | provider response ↔ tool preparation | Provider terminal persistence observes assistant completion while tool preparation may fail afterward. | One request may have one durable terminal. | Preparation/cancellation can fail after the response is already terminal. | A second terminal makes repair ambiguous. | `foreground-lifecycle-controller.test.ts` | FIXED/PUSHED (`4a40ff8ff`) |
| SI-002 | process leader ↔ descendant group | Bash/Python/process tools own a process group, not only its leader PID. | Terminal means every owned descendant has exited or been killed. | A descendant can outlive a leader or a group-kill attempt. | The harness reports completion while a child keeps locks, pipes, or CPU alive. | `process-group-wait.test.ts`, `run-process.test.ts` | FIXED/PUSHED (`4a40ff8ff`) |
| SI-003 | gateway registry ↔ provider instances | Registry startup/shutdown coordinates independent extension providers. | Each started provider must reach its own terminal. | One provider can hang or reject while siblings are stopping. | Sequential/unbounded teardown blocks the session or skips siblings. | `gateway-registry.test.ts` | FIXED/PUSHED (`4a40ff8ff`) |
| SI-004 | provider stream ↔ activity projection | Provider stream emits hidden reasoning and visible text to one UI lane. | First visible output and first hidden reasoning are distinct facts. | Hidden reasoning may arrive before readable text. | The UI reports a readable first token that never existed. | `activity-lane.test.ts` | FIXED/PUSHED (`4a40ff8ff`) |
| SI-005 | background terminal ↔ notifier retry ↔ session shutdown | A durable task terminal owns a bounded notification retry timer. | Terminal persistence survives notifier failure; shutdown owns the retry clock. | Shutdown can begin during retry backoff. | Disposal waits for an irrelevant clock or a late retry touches torn-down state. | `background-tool-task-controller.test.ts`; doctrine contract | FIXED/PUSHED (`a4efab29d`) |
| SI-006 | worker terminal ↔ parent notification ↔ disposed controller | Worker completion persists first, then notifies the owning foreground session. | A late callback may publish only while its controller is live and still owns the lane. | Notification completion can race disposal or lane reuse. | A stale worker wakes or mutates the wrong foreground owner. | `worker-notification-coordinator.test.ts`, `worker-delegation-controller.test.ts` | FIXED/PUSHED (`a4efab29d`) |
| SI-007 | write reservation ↔ blocked restore waiter | Reservation release publishes availability to waiters. | The subscription and lease are owned together and both terminate on shutdown. | Release or disposal can occur between the state check and subscription. | A missed wake leaves restore blocked until the watchdog. | `worker-write-reservation-coordinator.test.ts` | FIXED/PUSHED (`a4efab29d`) |
| SI-008 | managed/background lanes ↔ sibling cleanup | Lane teardown releases several independent resources. | Every release is mandatory even when a sibling release fails. | Any cleanup can reject before later cleanups run. | A held lane/resource permanently reduces admission and can deadlock later work. | `managed-lane-release.test.ts`, `background-lane-controller.test.ts` | FIXED/PUSHED (`a4efab29d`) |
| SI-009 | provider admission ledger ↔ independent holds | One session may own several provider/account admission holds. | `releaseAll` must attempt every owned hold. | An adapter can throw during one release. | Later holds remain counted until stale-heartbeat recovery. | `provider-admission-release.test.ts` | FIXED/PUSHED (`a4efab29d`) |
| SI-010 | local runtime registry ↔ session and sibling runtimes | A session controller owns several local runtime instances. | Disposal fences new work and stops every owned instance. | One stop can reject while siblings are still live. | Session exit leaves a model server/process resident. | `local-runtime-controller-reconcile.test.ts`, `session-oom-fixes.test.ts` | FIXED/PUSHED (`a4efab29d`) |
| SI-011 | worker recovery timer ↔ disposal | Recovery schedules delayed host-owned retries. | A disposed coordinator cannot resume a worker or retain a live timer. | Disposal can occur after scheduling and before callback delivery. | A late recovery mutates a closed lane/session. | `worker-recovery-coordinator.test.ts` | FIXED/PUSHED (`a4efab29d`) |
| SI-012 | worker outbox/transcript/usage receipt ↔ disposal | Worker persistence and receipt delivery finish asynchronously around lane terminal state. | No post-disposal write may create a new durable record. | Completion callbacks can arrive after cleanup begins. | Closed sessions acquire late transcript or accounting state. | `worker-conversation-store.test.ts`, `worker-usage-receipt-delivery.test.ts` | FIXED/PUSHED (`a4efab29d`) |
| SI-013 | background lane terminal persistence ↔ teardown | Background lanes must persist their terminal before ownership disappears. | Teardown waits for required terminal publication and independently releases siblings. | Abort, persistence failure, and cleanup can overlap. | A lane vanishes without a terminal or blocks all later disposal. | `background-lane-disposal-persistence.test.ts` | FIXED/PUSHED (`a4efab29d`) |
| SI-014 | worker shell lanes ↔ session disposal | Worker tool execution owns persistent shell lanes outside the foreground shell key. | Session disposal is not complete until every owned shell lane terminates. | A worker shell may still be closing when foreground disposal finishes. | The process/session exits while a worker shell retains descendants or handles. | `session-oom-fixes.test.ts` | FIXED/PUSHED (`a4efab29d`) |
| SI-015 | provider lifecycle ledger ↔ work-unit receipts ↔ claim correction | Provider persistence inserts a terminal between an assistant tool call and tool admission; work-unit accounting consumes the same branch. | The mutating assistant message is the authoritative work-unit start. | The provider terminal is appended after assistant persistence but before `noteMutatingCall`. | The command falls outside the receipt window; a failed push becomes “no push ran” and no correction turn executes. | `claim-delivery.test.ts`, `agent-session-bash-persistence.test.ts`; 26 focused tests plus 60 adjacent tests pass | FIXED/PUSHED (`a4efab29d`) |
| SI-016 | shell/task lane pools ↔ runtime builder ↔ session shutdown | Pool disposal starts several independent terminal closes; runtime and session shutdown await the aggregate. | Shutdown completion must mean every started lane close settled, while retaining all failures. | One close can reject before a sibling close resolves. | Fail-fast `Promise.all` lets `disposeAndWait` return with a live shell/resource. | `shell-lane-pool.test.ts`, `task-shell-sessions.test.ts`; 13 focused tests plus 45 adjacent tests pass (2 skipped); post-repair Jev gate passed | FIXED/PUSHED (`a4efab29d`) |
| SI-017 | process-matrix reconciliation ↔ master maintenance ↔ shutdown | Startup maintenance launches independent prune and interrupted-worker recovery mutations; `waitForIdle` and `stop` expose its terminal. | Maintenance is terminal only after every mutation it admitted has settled. | A prune can reject while a sibling recovery write is still pending. | Fail-fast nested `Promise.all` settles maintenance and shutdown before the durable recovery write, which can mutate state after stop. | `process-matrix-reconciliation-settlement.test.ts`; 2 focused plus 97 adjacent tests pass; post-repair Jev gate passed | FIXED/PUSHED (`a4efab29d`) |
| SI-018 | integrity hook timeout ↔ tool-gate arguments ↔ sibling extensions | The tool gate exposes live nested arguments through hook metadata; extensions run sequentially under a response timeout. | Hook context is an inspection snapshot, never extension-owned execution state. | A hook can retain the context after timeout, or mutate it before the next extension runs. | A timed-out advisory hook rewrites the already-admitted command after the decision, or one extension changes what a later validator sees. | `integrity-hook-context-isolation.test.ts`; 3 focused plus 67 adjacent tests pass; post-repair Jev gate passed | FIXED/PUSHED (`a4efab29d`) |
| SI-019 | staged production source ↔ affected-test selection ↔ full CI | The closure-surface test reaches `AgentSession` through the SDK harness, while the commit gate selects direct importers plus explicit `@guards`. | A test that verifies a transitive composition owns an explicit guard on the production boundary it measures. | The commit can pass its bounded affected tests before full CI traverses the wider import graph. | Main turns red on Linux, Windows, and the coverage gate even though the deterministic failing test existed locally. | CI run `36250826239`; selector probe changed from 20 tests/closure absent to 21 tests/closure present; focused 7 tests pass | FIXED/PUSHED (`2f03eb79e`) |
| SI-020 | shell execution aggregate ↔ independent terminal promises ↔ strict disposal wait | The session owner eagerly starts Windows-engine, persistent-shell, and lane-pool disposal, then publishes one aggregate terminal. | Aggregate rejection is terminal only after every admitted disposer settles. | One public disposal port can reject while a sibling terminal remains pending. | Fail-fast aggregation reports teardown failure while another shell resource still owns a process or handle. | `shell-execution-session-settlement.test.ts`; 2 focused tests plus 47 adjacent tests pass (2 skipped); post-repair Jev gate passed | FIXED/PUSHED (`2f03eb79e`) |
| SI-021 | extension generation ↔ `pi.exec` ↔ factory timeout/reload disposal | One extension API generation admits child commands through the shared process adapter. | Every admitted command belongs to that exact generation; caller cancellation remains an independent input. | A factory can time out or reload can dispose the generation after the command starts. | API invalidation blocks future calls but the already-started process outlives its owner and can retain CPU, pipes, or descendants. | `extension-exec-lifecycle.test.ts`; 3 focused tests plus 42 adjacent tests pass; post-repair Jev gate passed | FIXED/PUSHED (`2f03eb79e`) |
| SI-022 | package update coordinator ↔ npm scope batches ↔ bounded Git workers | One update starts independent user npm, project npm, and Git mutations; the Git branch owns a bounded worker pool. | Update failure is terminal only after every already-started mutation and active worker settles. | One npm batch or worker can reject while siblings still mutate install roots and Git checkouts. | Reload, retry, or removal can begin against directories that the failed update is still changing. | `package-update-settlement.test.ts`; 4 focused plus 151 adjacent tests pass, including single/multiple failures and out-of-order success; post-repair Jev gate passed | FIXED/PUSHED (`7ed1ea0ec`) |
| SI-023 | file-store managed files ↔ memory lifecycle coordinator ↔ later providers | File-store initialization admits general, user, and project managed-file mutations; `MemoryManager` intentionally continues after an ordinary provider error. | A provider initialization error is observable only after every mutation admitted by that provider call settles. | One managed-file initializer can reject while two sibling writes remain pending. | The manager starts a later provider and publishes initialization completion while failed-provider writes still mutate disk or emit stale notices. | `memory-initialization-settlement.test.ts`; 3 focused plus 48 adjacent tests pass, covering downstream ordering, success snapshots, and multiple failures; post-repair Jev gate passed | FIXED/PUSHED (`187cef9ff`) |
| SI-024 | collaboration recovery workers ↔ publication failure ↔ single-flight reattachment | Session restoration eagerly admits up to four durable-job recovery workers; the extension holds one aggregate reattachment promise. | Reattachment ownership is released only after every worker admitted by that reconciliation call settles. | A control-handoff publication can throw while sibling backend probes remain pending. | Fail-fast aggregation rejects and clears the single-flight latch while older workers still mutate or publish, allowing an overlapping reconciliation. | `collaboration-recovery-fences.test.ts`; 5 focused and 48 adjacent collaboration tests pass, preserving eager admission, stale-result fences, and exact single-error projection; post-repair Jev gate passed | FIXED/PUSHED (`84c41b3a2`) |
| SI-025 | emergency-stop admission ↔ cancellation ↔ activity/trace projection | Provider admission emits a live wait lifecycle consumed by the Activity Lane and operator flow trace. | Every emitted wait start owns exactly one end before its admission call settles. | Cancellation can reject the active emergency-stop sleep before the stop lifts or its wait budget expires. | The provider request aborts, but the UI and trace retain a false active wait indefinitely. | `provider-admission-wait-lifecycle.test.ts`; 3 focused and 53 adjacent admission/UI tests pass across abort, normal lift, and timeout; post-repair Jev gate passed | FIXED/PUSHED (`59a19f291`) |
| SI-026 | account usage concurrency queue ↔ credential refresh ↔ provider I/O | The usage monitor queues a request captured for one account; built-in adapters resolve the provider's current credential immediately before remote usage calls. | A captured account key must remain current both when its queue slot opens and after asynchronous credential resolution. | The provider account can switch while the request waits for concurrency or credential refresh. | A stale request sends usage calls under the replacement account, wasting quota and delaying parallel work; the result is discarded only afterward. | `usage-overview.test.ts`; 13 focused plus 7 adjacent usage-command tests pass, including queue-switch, credential-switch, idle cleanup, and unchanged-account controls; post-repair Jev gate passed | FIXED/PUSHED (`bfbead434`) |
| SI-027 | residency fit plan ↔ Ollama eviction result ↔ replacement load | The residency arbiter applies required evictions through the Ollama adapter before admitting or loading a replacement model. | A fit plan is valid only if every eviction it depends on succeeded. | Ollama reports a keep-alive eviction failure as resolved `{ ok: false }`, not a rejected promise. | The adapter discards the failure, so the arbiter loads the replacement and returns `fits` while the old model remains resident, risking memory overcommit and OOM under parallel work. | `runtime-arbiter.test.ts`; 12 focused and 95 adjacent local-runtime tests pass, including failed-eviction/no-load and successful-eviction controls; exact post-repair Jev branch gates passed | FIXED/PUSHED (`30c5e2447`) |
| SI-028 | model eligibility reconciliation ↔ pending readiness/install/residency work ↔ runtime ownership | The local-runtime controller owns exact-model readiness generations, runtime objects, and their residency adapters across Ollama, Transformers, and managed Prism. | An asynchronous continuation may publish readiness or mutate a runtime only while both its exact-model generation and captured runtime object remain current. | Reload or profile reconciliation can remove a model while detection, start, install, download, server probing, or residency inventory is awaiting. | A stale continuation restarts or registers a retired runtime; a late Transformers start becomes orphaned outside the controller map and survives later disposal. | `local-runtime-controller-reconcile.test.ts`, `runtime-arbiter.test.ts`; 28 focused and 132 adjacent local-runtime tests pass, covering removal during detect/start/probe/inventory and retained-model controls; exact post-repair Jev gates passed | FIXED/PUSHED (`6a0f9bdb6`) |
| SI-029 | shared runtime registry ↔ async health/start transaction ↔ sole process handle | Ollama and Transformers runtime instances are reused across readiness and direct lifecycle callers; each instance owns one child handle and now one keyed start coordinator. | Equivalent current-generation starts share one transaction; conflicting store intent waits and re-probes; stop retires all pending continuations before touching the child. | Two starts can observe the server down together, or stop can run while the initial probe or post-spawn poll is pending. | Both starts spawn and the second assignment loses the first child handle, or a retired start spawns after stop and escapes later cleanup. | `local-runtime.test.ts`; 49 focused and 140 adjacent local-runtime tests pass across double-start, conflicting-mode, pre/post-spawn stop, fresh-generation, timeout, and sequential controls; zero production clones; exact post-repair Jev gates passed | FIXED/PUSHED (`5a880d6ea`) |
| SI-030 | shared readiness callers ↔ model-file publication ↔ Prism server ownership | Foreground, prefix-warm, compaction, isolated, and interactive lifecycle paths share exact runtime/model resources; the controller owns readiness state, file runtimes own final paths, and the Prism runtime owns one child handle. | Equivalent provider/server/model readiness and same-path transfers share exact-key transactions; conflicting singleton serve intent serializes; only a validated staged payload is atomically published. | Two callers can enter before confirmation, download, or child assignment; stop/reconcile can retire work during a probe or poll; another session can replace the same final path. | Duplicate residency/download work wastes capacity, two serves lose a child handle, a retired serve spawns after stop, or a failed replacement deletes/exposes the last-known-good model or security-sensitive pickle. | `local-runtime-controller-reconcile.test.ts`, `llamacpp-runtime.test.ts`, `runtime-process.test.ts`, `local-runtime.test.ts`, `needle-runtime.test.ts`; five deterministic red failures; 157 focused and 251 adjacent local-runtime tests pass across exact sharing, provider/path negative controls, conflicting intent, pre/post-spawn stop, fresh generation, retry, atomic validation, session construction, reload reconciliation, command, and interactive composition; exact post-repair Jev gates passed; full repository check passed | FIXED/PUSHED (`b0ff2e94a`) |
| SI-031 | goal requirement check ↔ cancellation/timeout ↔ process-tree terminal | Goal completion owns an observational child process and publishes its check result into durable evidence; `killTree` owns bounded graceful/escalated tree termination. | A timeout or abort terminal may publish only after its one termination operation settles; a failed/rejected kill remains explicit, while normal exit never enters termination. | Timeout, in-flight abort, or a signal already aborted at admission can land while the child still owns descendants; child exit/error can race termination. | Completion repair/retry continues while the old verifier still holds processes or resources, cancellation is ignored until timeout, or cleanup failure disappears from the evidence trail. | `requirement-checks.test.ts`; four deterministic red failures; 10 focused and 81 adjacent goal/evidence tests pass across deferred timeout/abort termination, pre-aborted admission, failed termination disclosure, and normal-exit control; exact pre/post Jev gates and full repository check passed | FIXED/PUSHED (`8fcf8bd38`) |
| SI-032 | collaboration helper admission timeout ↔ late readiness ↔ process-tree terminal | One finite helper process brokers a persistent specialist turn; its ready IPC admits the turn while `killTree` owns timed-out helper teardown. | Once startup timeout wins, readiness is fenced immediately, admission failure waits for bounded helper termination, and failed/rejected cleanup remains explicit. | A ready IPC message or helper exit can arrive while the timeout is awaiting tree termination. | The outer coordinator begins stop/retry handling while the old helper can still deliver the turn, or cleanup uncertainty is erased from the failure. | `collaboration-turn-process.test.ts`; three deterministic red failures; 12 focused and 208 adjacent collaboration launch/reuse/recovery tests pass across deferred, failed, and rejected termination, late readiness, successful admission, and exact-turn exit fencing; exact pre/post Jev gates and full repository check passed | FIXED/PUSHED (`5876fb78b`) |
| SI-033 | account-model refresh request ↔ credential/model inventory ↔ routing availability | The account catalog independently checks current provider credentials and publishes one complete provider-availability snapshot consumed by routing. | Only the newest requested refresh may publish, and its snapshot must remove providers absent from the newest authenticated inventory. | An older credential check can finish after a newer check, or configured authentication can disappear between refreshes. | The stale completion overwrites a newer valid verdict, or an obsolete rejection/listing survives logout and routes against the wrong account state. | `account-model-catalog-ready.test.ts`, `account-model-availability.test.ts`; two deterministic red failures plus sequential and abort controls; 4 focused and 11 combined adjacent tests pass; exact post-repair Jev gates, full repository check, and zero-clone gate passed | FIXED/PUSHED (`31a435e83`) |
| SI-034 | auth credential mutation ↔ model registry ↔ account-model catalog | Auth dialogs own credential mutation/completion; `AgentSession` coordinates the registry and catalog refresh; the catalog owns tracked-provider adapters and their availability snapshot. | Login/logout success may publish only after both session-owned model views reflect the current authentication state; unrelated providers must not trigger account checks. | Account checks can remain pending after credentials change while the dialog proceeds to model selection, model count, and success status. | A successful login/logout leaves stale rejection or listing state, so routing and UI reason about the previous account even though credentials already changed. | `auth-dialog-liveness.test.ts`, `account-model-catalog-ready.test.ts`, `typesafe-runtime.test.ts`, `account-model-availability.test.ts`; three deterministic red failures plus unrelated-provider and real-session controls; 42 focused/adjacent tests pass; the commit gate additionally passed 184 adjacent tests (8 skipped) and project typecheck; exact decomposed Jev gates, full repository check, and zero-clone gate passed | FIXED/PUSHED (`fe7b3cd5f`) |
| SI-035 | gateway provider identity ↔ async start/stop ↔ replacement/restart | `GatewayRegistry` owns provider identity, desired start requests, bounded lifecycle attempts, and per-provider quarantine across channel and scheduler adapters. | Only the newest still-current provider request may remain active; an unresolved timed-out start or stop quarantines that provider identity without blocking independent providers. | Initial shutdown, same-name replacement, or restart can occur while start/stop is pending or after its bounded wait times out but before the raw callback settles. | A stale start activates after shutdown/replacement, or stale cleanup/stop deactivates the newer generation; duplicate listeners, sockets, jobs, or processes then survive or disappear behind a terminal registry state. | `gateway-registry.test.ts`, `agent-session-runtime-events.test.ts`; four deterministic original-source red failures plus one rejected-start cleanup preservation failure during repair; 13 focused and 29 combined adjacent tests pass across fulfilled/rejected replacement, initial stop, timed start/stop restart, stopped late cleanup, idempotent registration, and independent-provider progress; exact post-repair Jev gates, full repository check, project typecheck, and zero-clone gate passed | FIXED/PUSHED (`8332537af`) |
| SI-036 | bounded worker terminal ↔ admitted tool mutation ↔ restart suspension | `WorkerAttemptExecutor` owns admitted tool callbacks and its live mutation ledger; `runWorker` owns bounded claim projection; `WorkerDelegationController` owns synchronous teardown and restart suspension. | One seal transition freezes unresolved mutation exposure and pending tool-selection observations before terminal claim projection or restart suspension. | Cancellation or timeout can return without joining a non-cooperative provider loop; session disposal can suspend the worker before that loop unwinds. | The terminal claim or restarted conversation omits a real or still-possible write, while a late callback mutates durable progress, objective ownership, learning, review, or supervision after terminal publication. | `worker-attempt-executor.test.ts`, `worker-runner.test.ts`, `background-lane-disposal-persistence.test.ts`; two independent deterministic red failures plus pre-terminal, abandoned-invocation, selection, and cancellation controls; 101 focused/composed and 41 adjacent controller/resource tests pass; the commit gate passed 355 affected tests (8 skipped) and project typecheck; exact pre/post Jev gates, full repository check, and zero-clone gate passed | FIXED/PUSHED (`849fd6054`) |

## Rejected candidates

| ID | Candidate | Why rejected | Evidence |
|---|---|---|---|
| SR-001 | Provider lifecycle records break the compaction loop's trailing-compaction guard. | Coding-agent supplies a compactable branch that removes lifecycle entries and reconnects ancestry before the generic loop inspects the tail. | `CompactionController.getRawCompactionBranch`, provider replan tests |
| SR-002 | A dispatched tool failure makes the agent loop return before sibling tools settle. | Each dispatched promise catches its failure into the batch-owned failure slot; the final `Promise.all` therefore drains all dispatched calls before result publication. | `agent-loop.ts` partition drain and tool concurrency tests |
| SR-003 | Work-unit admission needs a fallback anchor when a tool gate receives an unpersisted assistant message. | Production tool execution persists the live assistant object before tool admission; accepting an unpersisted object would restore the physical-leaf ownership bug fixed by SI-015. Two direct-gate integrations fabricated objects, so both now establish their anchors through faux-provider turns. | `dogfood-safety-closure.test.ts`, `agent-session-closure-surfaces.test.ts`; focused and adjacent ownership tests; Jev classification gate passed |
| SR-004 | The nested Windows-engine `Promise.all` can reject before its sibling coordinator terminal settles. | Both inputs currently trace to resolve-only `PersistentProcessCoordinator` terminal promises; the lane pool's production disposer is the same resolve-only engine terminal. The unnecessary nested edit was removed. | `persistent-process-coordinator.ts`, `shell-lane-pool.ts`, `windows-shell-engine.test.ts` |
| SR-005 | Python stream cleanup can return before one temp file closes because it uses `Promise.all`. | `OutputAccumulator.closeTempFile` performs synchronous close, captures I/O errors into result state, and never rejects; both close calls finish before their already-resolved promises are joined. | `output-accumulator.ts`, `python-tool.test.ts` |
| SR-006 | A worker write must lose repository ownership when the worker's final lane status is blocked, failed, or budget-exhausted. | The mutation ledger records who produced the current bytes, not whether the worker's task claim was accepted. Admitted typed writes are recorded at tool completion, the outer fingerprint observer rejects undeclared deltas, and objective proof/delivery gates independently decide whether owned bytes may be committed. Gating authorship on terminal status would instead misclassify real worker bytes as another session's work. | `worker-attempt-executor.ts`, `repository-mutation-observer.ts`, `objective-mutation-ledger.ts`, `objective-execution-controller.ts`; Jev selected path authorship (`1.00` confidence) and found no false-ownership witness (`0.91`) |
| SR-007 | Local-runtime composition constructs `ForegroundLifecycleController` with a lifecycle index missing provider-request fields. | The failure came from launching Vitest at the monorepo root, outside the mandated package-root configuration. That unsupported invocation omitted `pi-source` resolution and loaded stale `packages/agent/dist`; direct conditional resolution and the configured package-root run both load current source. No production fallback was added. | Root-run failure at `inspection.providerRequestTerminals`; direct `node --conditions=pi-source` inspection includes every provider field; package-root Vitest passes 31/31 affected session tests and 251/251 adjacent local-runtime tests; Jev classified runner artifact and fail-closed policy at `1.00` confidence |

## Jev review log

Raw request/response artifacts remain outside the repository. This ledger records their immutable bundle and
request hashes, resolved model, answer probabilities, and confidence; deterministic regressions remain the proof
gate.

| Finding | Phase | Model and immutable evidence | Judgments | Disposition |
|---|---|---|---|---|
| SI-015 | Post-repair verification | `jev-1.13.0`; bundle `afeb7921da2566ce48fd947a83427add95ad65c4a6642ce3e3a1399fbc853737`; request `40e05747e06434639ce14636ab25f5064fb158a04194d03713a04aa177d52504` | Receipt/correction `preserved` (`0.99` probability, `0.98` confidence) | Accepted at the `0.90` gate after adding the persisted-ordering and end-to-end failed-push regressions. |
| SI-016 | Pre-repair discovery | `jev-1.13.0`; request `496b7c225efd532cac0aeb297bc6aba45a7af10e20626fcf142dff2282337fbe` | Shell boundary leaned early-return (`0.53`, `0.29` confidence); task boundary leaned waits-all (`0.64`, `0.46` confidence); transitive boundary leaned cannot-finish-early (`0.54`, `0.30` confidence); top-level negative control `preserved` (`1.00` confidence) | Not accepted. Dissent retained; two deterministic tests subsequently reproduced early settlement at the nested boundaries. |
| SI-016 | Post-repair verification | `jev-1.13.0`; bundle `afeb7921da2566ce48fd947a83427add95ad65c4a6642ce3e3a1399fbc853737`; request `5d8f1ad0d5dbadee7889ab7b17925e068a545c276e96c9452a6a370795dc3982` | Nested completion `preserved` (`0.98`, `0.96` confidence); failure preservation `preserved` (`1.00`, `1.00`); session barrier `cannot_finish_early` (`1.00`, `0.99`) | Accepted at the `0.90` gate after the all-settlement repair and deterministic regressions. |
| SI-017 | Pre-repair discovery | `jev-1.13.0`; bundle `29380e61431da988dd1ce28b138b5a86e795a496a5c6b86efac2442474f02eba`; request `af560992675b8072cc4bfb010a6364db642878fe30de5519974dfd2d17875bb5` | Early finish `can_finish_early` (`0.62`, `0.43` confidence); successful control `preserved` (`1.00`, `0.99`); post-stop mutation `possible` (`0.75`, `0.63`) | Not accepted. Dissent retained; the adversarial regression then deterministically reproduced shutdown returning while the recovery write remained pending. |
| SI-017 | Post-repair verification | `jev-1.13.0`; bundle `afdb9505ac995b9859128db3f835ab4e0acc43f38e0449f72d3828f6dd92ccd8`; request `b6300d1b0a23b6860f669676de9ac7a9538410728233c51ee3e481100a3ea8db` | Settlement barrier `cannot_finish_early` (`1.00`, `0.99` confidence); failure reporting `preserved` (`1.00`, `1.00`) | Accepted at the `0.90` gate after deterministic reproduction, all-settlement admission, and adjacent process-matrix verification. |
| SI-018 | Pre-repair discovery | `jev-1.13.0`; bundle `e610727ffbd5de04053b28ac31a548d36dd791aefd0c29c5bd5de89658986d5e`; request `09a871c859efa9e417a2926b1ac122990cdb7e004da911704aa51aef2ce5becb` | Late mutation `possible` (`1.00`, `0.99` confidence); sibling contamination `possible` (`1.00`, `0.99`); high-impact fail-closed control `preserved` (`0.97`, `0.95`) | Accepted at the `0.90` discovery gate; both mutation paths were then reproduced deterministically. |
| SI-018 | Post-repair verification | `jev-1.13.0`; bundle `5fd5db5c599de9b2f0d3d86de7d8a65ca9f1a3c93b7a1440d21fab5d3cdd11e6`; request `28e540f3de93e3981c06d8e5d336f193551a4851767f6e47ab55dc950b501c9a` | Caller isolation `prevented` (`1.00`, `1.00` confidence); sibling isolation `prevented` (`1.00`, `1.00`); high-impact policy `preserved` (`1.00`, `1.00`) | Accepted at the `0.90` gate after the two deterministic mutation regressions and adjacent integrity-system verification. |
| SR-003 / SI-019 | Post-CI classification | `jev-1.13.0`; bundle `40480b204c999cb7b676b96d30824c2c07df863b8f96ff1b69fcfe5967f37114`; request `e099d19857cea7a66ae2f0a005328a76571e7afd85aef9536c002f6e219196e5` | Failure `test_seam` (`1.00`, `1.00` confidence); proposed test repair `preserved` (`0.98`, `0.97`) | Accepted at the `0.90` gate. The production fail-closed owner remains unchanged; the composed test now supplies the canonical persisted identity and declares the guarded production boundary. |
| SI-020 | Pre-repair verification | `jev-1.13.0`; bundle `424389ebd0a9a02decc21a59a32a20522c47c2fc9e57da3393e2c71283dfd028`; request `d41791a94ce6309ac3c029d2cefe51cb991004f6653d940f65004f5373556db3` | Defect `confirmed` (`0.94`, `0.91` confidence); abstract repair selected but uncertain (`0.80`, `0.71`) | Defect accepted; production edit paused until exact eager-start wiring passed a narrower follow-up gate (`0.96`, `0.95`; request `dd2d4e677bc9e7569ce83ad4bdf511ae3cb18df00829e1316ff77f54082f40ef`). |
| SI-020 | Post-repair verification | `jev-1.13.0`; bundle `819dc99a3846c0e5acef666d651a7beb61bd42bba77ae7d639f049e3cf6c3cb2`; request `c2b08ec9c3453351c46d04c1789d13f4c8e12d639a07a3be837afdb8b0e560b2` | Settlement barrier `cannot_finish_early` (`0.99` confidence); single failure `preserved` (`1.00`) | Accepted at the `0.90` gate after deterministic rejection-plus-pending and success controls. |
| SI-021 | Pre-repair verification | `jev-1.13.0`; bundle `bfb0b81e60f6aa7eac37596e0cdabaa76dc700f70277790841842fcbc274c7fb`; request `aa74d48ed8a8903630e34a3a192a31405a2d81a5e743afd9036f16ab2079ccf9` | Defect `confirmed` (`0.99`, `0.98` confidence); combined repair selected but just under gate (`0.92`, `0.89`) | Defect accepted; production edit paused until host-first disposer order (`0.99`, `0.98`) and caller-signal composition (`1.00`, `1.00`) passed separately (request `8ff67d0a282867c8e608ddd9a2f23ef68f11f25c5b29fa04fed4513cc61a8ee7`). |
| SI-021 | Post-repair verification | `jev-1.13.0`; bundle `819dc99a3846c0e5acef666d651a7beb61bd42bba77ae7d639f049e3cf6c3cb2`; request `bd224a8807f4d1680d98ef1fbbb2e9c7f95b19f749ae3cb744b572df6449a52d` | Generation cancellation `prevented` (`0.91` confidence); caller cancellation `preserved` (`1.00`) | Accepted at the `0.90` gate after timeout, ordinary disposal, and caller-abort regressions. |
| SI-022 | Pre-repair review | `jev-1.13.0`; bundles `2ad484b33799ff555f2238ca9709af7fe483f56dc1f2555a132a3d13d143ef85`, `05d8791ed60cd0aea905219a075b5ffec8028ef16636043f11f163a8ba77e8e0`, `2777766686c3b9c4e7fdfa60f291cca5f9d51198f4f5da3474eef4fd0296f106`; initial request `91af0e9534843981bf827471646df70e6968ca253de27d2d76a59bd7ab4a3619` | Initial defect `confirmed` (`0.76` probability, `0.63` confidence) and repair `supported` (`0.92`, `0.88`); decomposed outer repair passed (`0.98`, `0.97`), but repeated timing/worker questions stayed uncertain and the final worker timing choice contradicted the executable result (`still_pending`, `0.55`, `0.33`). | Not accepted as a positive gate. The inconsistent low-confidence judgments are retained; two deterministic red tests independently proved early rejection, and the repair is gated by focused and adjacent executable evidence. |
| SI-022 | Post-repair verification | `jev-1.13.0`; bundle `7a8ccf92a82e61444c25cb23c65e07e084df8f720fd9466d0700ee6c3e636c06`; requests `b27f547eb87d338e63d28ea923f60e5f2846a0bc17e82c1461b5143fa707aaf8`, `3d8da61b2776754e822cce5908664f38725cd6913303c0cdc95b51a870fd9986` | Outer settlement `cannot_finish_early` (`1.00` confidence), single failure `preserved` (`1.00`), worker settlement `cannot_finish_early` (`1.00`), indexed results and failure projection `preserved` (`0.99`). | Accepted at the `0.90` gate after current-source all-settlement repair plus single-failure, multiple-failure, and out-of-order-success regressions. |
| SI-023 | Pre-repair review | `jev-1.13.0`; bundles `2a53722d82db3bce45284f9ee75b88f508c599fc129e47ef067edacf989c154e`, `417d35225c3e89f9439f8595aefa0b0e904442af33f3f19c582184e07798ae52`; initial request `5e88eb2b06008d8d946518c8f3b1c177a195d5a92ec07e73e4c2e7dda8fa679c` | Combined defect `confirmed` (`0.79` confidence) and repair `supported` (`0.89`); requested decomposition again chose inner `already_rejected` (`0.63`), manager `proceeds` (`0.88`), and repair `establishes_all` (`0.84`) below gate. | Not accepted as a positive gate. The choices and uncertainty are retained; the composed red test independently observed manager completion and downstream initialization while two sibling writes remained pending. |
| SI-023 | Post-repair verification | `jev-1.13.0`; bundle `82a8dcb9cc5535f8a1c15a6c9cd8e095c305c2b7bb9e15772df742077740763e`; request `64da5b19e1a1b268868a9751061c79db572d184be597137fe25d57533b05766c` | Downstream barrier `cannot_advance_early` (`0.99` confidence); snapshot and failure projection `preserved` (`0.98`). | Accepted at the `0.90` gate after the file-store settle-all repair and composed, success, and multiple-failure regressions. |
| SI-024 | Pre-repair review | `jev-1.13.0`; narrow bundle `d4ce4d94c212a04c45ea920d3b68cc0df82bd6b739990cc5ede504ef8bcdfa44`; request `23146eda96e500aeaba39c64b79930d1c33579b273a5c3d4c85e1f045091dcc6` | Exact T0/T1/T2 terminal order selected `rejects_at_t1` (`0.79` probability, `0.70` confidence), below gate. | Not accepted as a positive gate. The uncertainty is retained; the deterministic red test observed reconciliation settle while three admitted workers remained pending. |
| SI-024 | Post-repair verification | `jev-1.13.0`; bundle `bb373d2d58f3ff87a1c715030db6004e5f1dfecb52c74221b64b8b536f0e69fb`; request `7c8bed0bcc38b7a0e39eb9d98591d51fab065d75a0fe3611492514b8b14038dd` | Eager admission, settlement barrier, and failure projection `passes` (`0.99` probability, `0.98` confidence). | Accepted at the `0.90` gate after exact-source repair and focused concurrency/staleness regressions. |
| SI-025 | Pre-repair review | `jev-1.13.0`; bundle `27e9193565145c69b6fc66df3e8d1d1cddff06e00f55bc12cf7e91e149274281`; request `bba3428b78ce6e4e011f37dd2ab1a8c5eebb7cf4e841015b0239446b3fe5b4f7` | Cancellation defect `confirmed` (`1.00` probability, `0.99` confidence); combined repair supported but uncertain (`0.81` confidence). | Defect accepted; production edit paused until decomposed abort (`0.93`) and timeout (`0.95`) preservation passed (bundle `c16beea17bcffa8f8f36428c408103475044f20702d2f19f612bb930b4b2397f`, request `774b0b9edf2718ef3259e97743da4fa3fc85a37d8b31f227e4188cfc598e7e4e`). |
| SI-025 | Post-repair verification | `jev-1.13.0`; bundle `4e664ce6247391eb22299630e25542851f36d4b14912f832ddd9d4479bb7f44d`; request `370295d1264d7c76f94f23db7eca2309104b9ed75195378a54732a65cbe4f55e` | Paired terminal lifecycle and policy preservation `passes` (`0.98` probability, `0.97` confidence). | Accepted at the `0.90` gate after exact-source repair and abort, success, timeout, admission, and UI projection tests. |
| SI-026 | Pre-repair review | `jev-1.13.0`; bundle `4cb5887396d65ae4734ff577682dcb5aa7e635e45c1a1e0528bb618105e3ec35`; request `f299bce9fa35f2f5a73d8e0a73ae3752a3b85080c1bf12710219e2e5f74c3698` | Both stale-send windows `confirmed` (`0.99` probability, `0.98` confidence); combined repair supported but uncertain (`0.57` confidence). | Defect accepted. The decomposed hypothetical repair stayed uncertain for queue (`0.52`) and adapter (`0.26`) but accepted cleanup (`0.93`; bundle `5e0cba9ad06d652156ed8f3152c19cde97ea3164734dd5ab086c71e6b4e0195b`, request `f499d88dd6681b543a056a7e7839e85302f0bb5d0b34dfce137113a314128a9a`); the prescribed deterministic routes reproduced both I/O calls and the success control. |
| SI-026 | Post-repair verification | `jev-1.13.0`; bundle `cd5baf201746188a3cb89125862b9da2bf9a76525d8fa2e11881225bd85b369e`; request `8ba354dd635f14db459bf5772041342874e7a4a0c06fe145892afcce58ecfa18` | Queue fence, adapter fence, stale cleanup, and unchanged-account execution `passes` (`0.94` probability, `0.91` confidence). | Accepted at the `0.90` gate against exact repaired source and both deterministic stale-I/O regressions. |
| SR-006 | Candidate classification | `jev-1.13.0`; bundle `6c2adadfa0a2020fbf40b74ed8730ec25fe022e2bbf8a29386c2cf10e3062033`; request `bbff1459897fcc5570ca4b2dffcd2acd24f41f8c5c8a171b8667f16a7726f150` | Terminal gating rejected in favor of path authorship (`1.00` confidence); no false-ownership witness through the supplied fingerprint/digest flow (`0.91`); duplicate-path interpretation remained uncertain (`0.72`). | Rejected the terminal-status repair. The uncertain duplicate writer remains an audit lead rather than evidence for a behavior change. |
| SI-027 | Pre-repair review | `jev-1.13.0`; bundle `ec2ed4c5a5e53e12cd8f9906009fb9bcdb72842ac656c498cecd9982de7e4777`; request `ad2322ba92619208e6c2cf0d838b71a618c0fc1e82eb68814314fa402d915824` | Failed eviction followed by replacement load `confirmed` (`1.00` confidence); the combined repair judgment selected correct but remained uncertain (`0.84`). | Defect accepted. Production edit waited for decomposed failure-stop (`0.97`) and success-preservation (`1.00`) gates before implementation. |
| SI-027 | Post-repair verification | `jev-1.13.0`; bundle `74d9c2f5b80a20d6229b593981d9c167d645d097089a9be3415e2d0ff2641752`; request `72ff1b47c9ba8021ab180c421468b49e55036c1a60c3ce4e9a69ce6ed3e937f2` | Exact failed-eviction branch `preserved` (`0.99` confidence); exact successful-eviction branch `preserved` (`1.00`). | Accepted at the `0.90` gate after the adapter fail-closed repair and focused/adjacent executable evidence. |
| SI-028 | Pre-repair review | `jev-1.13.0`; bundles `59a573d01ccd01a3b70840c2d5c397a06ac6bbf424db782f816fb7085ce0f2b8`, `8e8bdf2915a75dcff76f0e70c728d5be7f8064de7123f4ca50aa58981b6b1f57`; requests `f93c37c456aa352d02521834d39e17a3077ce66b3159bc5261d8c1c1a961832c`, `007d1288ad0746a3c0d93c1831f9c710769bad80a1a6fafe194c7c24f7f3cba9` | Broad defect (`0.48`) and repair (`0.35`) judgments were uncertain, while provider-family scope passed (`0.92`). Decomposition confirmed stale detect (`0.95`) and late-start (`0.96`) continuations, and supported the fence (`0.98`) plus retained-model control (`0.99`). | Broad judgments were not accepted. The decomposed gate and two deterministic red tests established the defect and bounded repair before production changes. |
| SI-028 | Interim post-repair verification | `jev-1.13.0`; bundles `1cabd1c62c7039d15be41141b8144d4135d906b9ac547f948e5184e1be51f493`, `0838fec8d26faafaa9ceb672143a8f353a4f17e8ad3a2025dbffb351f1bf63d1`; requests `1e0a32fd45639e4483a8d3d8cca022e366cac00cc2b477062e092337439b2eed`, `316553f0a9f8a4a8bd303bd4c07f6d09aacb2019ae7c9f4812204cd3aa33d1c7` | Exact Transformers fencing/cleanup/control passed (`0.99` confidence), while the combined sibling-family judgment selected pass below gate (`0.87`). Its decomposition passed for Ollama (`0.98`), Prism (`0.97`), and residency cancellation (`1.00`). | Accepted only after decomposition, then superseded when the clone gate required an ownership-helper refactor. The uncertainty remains recorded. |
| SI-028 | Post-refactor exact verification | `jev-1.13.0`; bundle `f8c147282a46eb360853bf279c584ae3a2109275e1ae0f893095e781dd086621`; request `a76bd8edaaa82a0d7b540afb8a36dcfed88e83dd769523e4a475462c2faaec2a` | Exact Transformers fencing/cleanup/control passed (`0.98` confidence); Ollama, Prism, adapter removal, and residency cancellation passed (`0.94`). | Accepted at the `0.90` gate against the final zero-clone source. Exact-source repair plus removal-during-await and retained-owner evidence does not establish whole-runtime correctness. |
| SI-029 | Pre-repair review | `jev-1.13.0`; bundles `8790e7a2ac5d8124eabeb00eb120dda3950cf04ccfdfcebdbd1657fed0b1bb12`, `93393215cabd281033f95ad8fde5c0ec56a83d54015353f102bf402a2442754d`, `333c5d6ad8f0f30847df6074e87ee32dab5f26b410f022f9f8c0dd44f9829d09`, `0ca24fa41be801f1b1e4eb0c38a151662b74003c033abb3de3556c870d2d45db`, `acc52511f58c20e5482b0b1b8e728d4b95cee887b5d6410a90e480a1f34288d6`; requests `c63aef9e049cbe6092274a890f5673e57c2c991b8b9a1348ba3c9e5e17d5a1ff`, `91ac2494010e02bce4bac3addba2ffebe4a8682b62e31069512328a5dc4b606c`, `58baa7c3cf5778d58f6913e2228fa2fb190eec1585b623670b13802c3718f530`, `8cff151038991c16530e9383b55ac741fb0e0f91a5710148eb851cf8ca0acb10`, `998d0802b3013a73548f2059c7bacad1cd40ca8250fb19e84d5b3005870f8ea2` | Source-only double-spawn choices were uncertain (`0.54`/`0.41`), and combined design judgments stayed below gate. Decomposition passed same-key admission (`0.94`), store-mode keying (`0.96`), fresh restart and identity cleanup (`0.96` each), post-spawn retirement (`0.90`), timeout cleanup (`0.99`), and the exact temporary pre-spawn proposal (`0.98`). | Broad judgments were not accepted. Four initial deterministic red failures proved both provider double-spawns and post-stop spawns; additional conflicting-mode, post-spawn, and fresh-generation controls bounded the repair before production closure. |
| SI-029 | Post-repair verification | `jev-1.13.0`; bundle `8242129496ba22f87e94db4fe44b68b0e81dcd156d7e6ef169bfcecf5c57dbcc`; request `42a39de047906db67080e26df6905247668e7fc82b16f20bb1b35e00fbbca7f3` | Exact transaction ownership and conflicting-mode serialization passed (`0.97` confidence); stop retirement plus later-generation liveness passed (`1.00`). | Accepted at the `0.90` gate against the final source and 49-test focused evidence. It does not claim serialization across distinct runtime instances. |
| SI-030 | Pre-repair review | `jev-1.13.0`; bundle `0abaf83a0569883160e4b2b7a8cf4d0c4952be358031e60700c27cc50f98a05e`; request `c7a537f158990152b84e239d142a297853b6aa0b6ffec4d6fc786bb2b35b308d` | Same-path destructive overlap was `confirmed` (`0.92` confidence) and layered ownership selected (`0.94`); controller duplicate readiness (`0.45`) and serve lost-handle (`0.30`) choices were uncertain. | Accepted only for the file-path candidate and ownership decomposition. The uncertain process/readiness candidates remained open until deterministic barriers reproduced both defects. |
| SI-030 | Post-repair decomposed verification | `jev-1.13.0`; bundle `043bbd4a455ecb7d9a0a6032a401d862cebc23a4ebd8d8ab5b43b9df734cad2d`; requests `a821ed878fc3dfb63871b43397c18c818e4f4820e81d02ff4758623ba755cd8e`, `77f4ea812405e7bd4c9f9e7b87b037875226f099bc13bda5a09f4544df7379a3`, `456ea8749d08714cb811ff665791ad4ab9c4e017a59ba845aaaec61eb2e5fba5`, `ddb6df2a964d6bd38a16c96389396835c14a28b9816ca755472578fe2227e9cb` | Readiness ownership passed (`0.96` confidence), serve ownership/liveness passed (`0.93`), transfer/publication passed (`0.99`), and the extracted Ollama/Transformers start coordinator preserved behavior (`1.00`). | Accepted at the `0.90` gate after exact-source decomposition and 157 focused executable tests. Cross-process transfer deduplication is not claimed; unique staging and atomic publish protect integrity when separate processes overlap. |
| SR-007 | Candidate classification | `jev-1.13.0`; bundle `4f57931a9cbdba5a9665b6610fe9ad866c5e118d770ed6041548d0058e177dff`; request `becacb77a3b43596ebbd7eed2b087ae13a05255212a5810c66b2da244a854d90` | Runner artifact selected (`1.00` confidence); mandatory lifecycle contract should remain fail-closed (`1.00`). | Rejected the production-defect candidate after exact source/dist comparison and configured-runner executable evidence; no fallback added. |
| SI-031 | Pre-repair review | `jev-1.13.0`; bundle `d265fb753f0aa408e8817710ff9616317c933c2efa7b50b9264beca6355ad2db`; request `5d06d6c051d4725b6780723eaff9859c85efa63bf1b6631c9e52f15651a70dc1` | Early timeout/abort terminal confirmed (`1.00` confidence); single-entry await plus failure disclosure supported (`0.99`). | Accepted at the `0.90` gate; deterministic deferred-termination tests then reproduced both cancellation paths before production repair. |
| SI-031 | Post-repair verification | `jev-1.13.0`; bundle `7895dbd4cb714ba813dded029b8ac6728485ebecb5f08d1573a98084a2ef9481`; request `44f5da5578c4433896751610e1453458007d8d69f0983dcb9bc96cb23648741e` | Timeout/in-flight abort/pre-abort settlement ordering passed (`1.00` confidence); reason, failure disclosure, and normal-exit policy passed (`1.00`). | Accepted at the `0.90` gate against exact formatted source and deterministic lifecycle controls. |
| SI-031 | Final test-isolation verification | `jev-1.13.0`; bundle `45cfbed5ffaa047cfbd7ed35ce4b69dd1db96e37e0fad359c86c356c6a5dbadd`; request `f73ca5e4ce1cabbbba35f1c741adcb7fdb0a56e269f3e6cfc541b19be958f5e5` | Final lifecycle contract plus real-child-process test isolation passed (`0.97` confidence). | Accepted after the required `@isolated` marker moved the regression out of the shared module-cache project. |
| SI-032 | Pre-repair review | `jev-1.13.0`; bundle `6d89a8a54c7b92d981fd5e1caddba464a0891826280e00e905640049a05d191a`; request `66da7223dda5b0d97dad49d0b4f7aa2c0f3eb422695cfb2f560108ab513c59f8` | Early timeout publication and hidden failed cleanup confirmed (`1.00` confidence); immediate readiness fence plus awaited termination and disclosure supported (`1.00`). | Accepted at the `0.90` gate after deterministic deferred/failure red evidence. |
| SI-032 | Post-repair verification | `jev-1.13.0`; bundle `642b2bc8aa825051ec921ebe854ef3f984b18d25ec88ae1257d2eddf4bcd00dd`; request `d0f0c631a2ac41b6571b3ac54b8f9273f1da11cb1d2a69d2a2cc14cda9320c0c` | Final timeout barrier, late-readiness fencing, failure disclosure, and ready-success preservation passed (`1.00` confidence). | Accepted at the `0.90` gate against exact source and focused lifecycle controls. |
| SI-033 | Pre-repair review | `jev-1.13.0`; bundles `2030a11b9e34b9926e7e3b5274129326cde0576e0c07d009f1b80137ddfd3191`, `230c885640c862af3dd48e71df507af7f3f7f48db2d6f4d4f54466d67a721851`; requests `9eb26218713ece44e069fad5d0b96674e59dfc83a075543e2acf7b94d83bdde5`, `e4fef48df5da2d7567847bb679cfdc91980c8ebfac9efcb9540dcc6c3c18a227` | Stale overwrite confirmed (`0.96` confidence); the initial abstract generation repair (`0.85`) and replacement (`0.66`) stayed uncertain. The added auth-removal witness gated whole-snapshot replacement (`0.99`) and adjacent preservation (`1.00`), while the abstract generation proof remained uncertain (`0.84`). | Only the bounded defect, snapshot replacement, and preserved paths were accepted pre-repair. Generation uncertainty was retained until Jev could inspect actual code and executable evidence. |
| SI-033 | Post-repair verification | `jev-1.13.0`; bundle `fa13d28746e138511d3abbff512e0f01fdea6fce02902242ef9d25d0bf3afa12`; request `d0e9de060fd06e2279ad7d482b5fcc4314a7af60324e12883b37a19622a6fd6d` | Implemented generation fence passed (`0.99` confidence); complete snapshot replacement and provider-check parallelism passed (`1.00`); abortable readiness and refusal precedence stayed preserved (`1.00`). | Accepted at the `0.90` gate against exact repaired source, both completion orders, the auth-removal regression, and adjacent controls. |
| SI-034 | Pre-repair review | `jev-1.13.0`; bundle `c272f8f6a0dbd28271b7b83ef8c8d152cd36e39b06937f8524a955c931df0d9d`; request `32bcf77c9e76a77029a7ff60317af00560f396083325e44ad5db4cdb82d59942` | Missing auth-to-catalog handoff confirmed (`1.00` confidence); unrelated-provider control passed (`0.93`); split-owner repair stayed uncertain (`0.80`). | Defect and negative control accepted. The uncertain ownership design was not accepted and was revised before implementation. |
| SI-034 | Ownership-design follow-up | `jev-1.13.0`; bundle `4c97b852c1829db1e21eeedff0baaa962c65133f6cea367187a0a862dbc4583c`; request `53c1e2b009a686989b2c09a099d1662fcf0430773a93303aab3de309527f7d54` | Abstract single-owner coordination (`0.64`) and ordering (`0.82`) remained below the confidence gate. | Not accepted. Deterministic red evidence and exact implemented-source review were required instead of promoting an abstract judgment. |
| SI-034 | Post-repair review | `jev-1.13.0`; bundle `9a0915a6f5090d0dd9b33629b3b94876d35ef7c8250f21cae2305d9550af5aec`; request `55b3255b3855f6465af7a01a556cb95bd545cb08ec74dd031605245c88522ced` | Implemented refresh-before-publication order passed (`0.96` confidence); unrelated-provider preservation passed (`1.00`); broad ownership remained uncertain (`0.76`). | Exact ordering and negative control accepted. The broad ownership judgment was retained as uncertainty and decomposed. |
| SI-034 | Ownership decomposition | `jev-1.13.0`; bundle `40aef23f2b5e5cda7f356e9e7ac512b5856fb1f519363f865ffec1c4d41e4dcd`; request `1dd021cf6e99c096286e07997f301bfdc62253576f1c632d80528000924f66e5` | Generic login/logout paths have no refresh bypass (`1.00` confidence); the session coordinator owns the complete registry/catalog sequence (`1.00`). | Accepted at the `0.90` gate against exact source and the composed logout, deferred login, real-session, and unrelated-provider controls. This does not prove external credential mutations outside the audited interactive paths. |
| SI-035 | Pre-repair review | `jev-1.13.0`; bundle `e76ba386d6cf34e1023fa2e477250340dd045b385bdebaa9d810cb16398c6c68`; request `19a09537b7ec238b7874a9fc8230ad04bbb67901d92c7f19dfb3986ea328cdee` | Replacement activation (`0.90`) and stale restart cleanup (`0.96`) were confirmed; bounded independent progress was preserved (`0.99`). Initial shutdown (`0.74`) and the broad coordinator proposal (`0.76`) remained uncertain. | Accepted only the gated defects and negative control. The initial trace and repair design were decomposed rather than promoting low-confidence judgments. |
| SI-035 | Transition decomposition | `jev-1.13.0`; bundle `1dc819fbb582e9ec03c69143e48cd8a397fb55141139b0f3f5e1d4714c49ae19`; request `099a0525f4ec8396f9e2d2afebf5655b9e8fe5b6c2746b097930c4c20bf24aa4` | The exact initial post-stop activation trace passed (`0.92`) and bounded progress passed (`0.98`), while prose-only settled (`0.61`) and timeout (`0.71`) repair transitions remained uncertain. | The exact defect/control gates were accepted. Production repair relied on deterministic regressions; proposed state-machine judgments remained explicitly unaccepted until Jev inspected actual code. |
| SI-035 | Post-repair verification | `jev-1.13.0`; bundle `f337a7998766ecc32c3647ea76fd2eb3527f2d12af0087dbc002ab96655a0cbc`; request `5aa2aae039c00e10d07959e7873cd6766de17d1e1f262eb8954cd8f6b8aa08c6` | Obsolete settled-start cleanup passed (`0.98` confidence); timed-start ordering passed (`0.95`); timed-stop ordering passed (`0.96`); bounded, stopped-state, and idempotent controls passed (`0.99`). | Accepted at the `0.90` gate against exact coordinator source and all lifecycle regressions. It does not claim correctness inside third-party provider callbacks or recovery from a raw provider operation that never becomes terminal. |
| SI-036 | Pre-repair review | `jev-1.13.0`; bundle `0b4f6f55a45ef9ec4495357a9f56ef6caed9ec38c5953a6d2db877d4d764532e`; request `21edf1f106195714a4a704bbabd68fcda7f1ab59c187d79128d31ce75fd8296e` | The defect was `confirmed` (`1.00` confidence), the abort-first shortcut was `insufficient` (`1.00`), the seal boundary was supported (`0.96`), and the negative control was preserved (`0.99`). | Accepted at the `0.90` gate after the narrow exact-source packet. The initial full-source request exceeded the service token limit and produced no judgment; it is retained as an incomplete probe, not evidence. |
| SI-036 | Post-repair verification | `jev-1.13.0`; bundle `12ac8df350ed74e4b8f53fe135a3b0fc4b132ecdf8ad36b758ac0ac4f24eeed0`; request `19a692305a8fcb6a7885f9566755e65beecd697bf24f0f6ab76fa7f6776298cc` | Bounded terminal fencing passed (`1.00` confidence); disposal persistence passed (`1.00`); pending selection terminalization passed (`0.97`); controls passed (`0.99`). | Accepted at the `0.90` gate against exact repaired source and both independent terminal paths. The seal discloses unresolved admitted write targets conservatively; it cannot revoke arbitrary third-party side effects. |

## Open coverage

- Shared session-branch consumers that infer meaning from adjacency or the physical leaf.
- Multi-resource shutdown paths that use fail-fast aggregation.
- Timer/subscription callbacks whose owner can be disposed or replaced while delivery is queued.
- Mutable “latest/current” identities read after an await instead of identities captured at admission.
- Completion and retry paths where durable terminal publication and in-memory cleanup have different owners.
- Composition tests that reach lifecycle owners only through harness helpers and therefore need explicit affected-test guards.
- Extension module top-level code remains a trusted, unsandboxed boundary: a module-import timeout bounds the loader's wait but cannot revoke arbitrary side effects the module started outside `ExtensionAPI`.
- Separate Pi processes can still download the same model concurrently. Exact in-process callers share work, while unique staging plus validated atomic publication prevents final-path corruption; cross-process bandwidth deduplication remains unimplemented.
- A non-cooperative worker tool may still alter filesystem bytes after cancellation. Admitted write/edit targets are conservatively disclosed and automatic delivery fails closed, but arbitrary third-party tool side effects are not revocable.
