/** A missing leaf or non-directory ancestor permits an alternate path lookup. Other I/O failures do not. */
export function isMissingPathError(error: unknown): boolean {
	return (
		typeof error === "object" &&
		error !== null &&
		"code" in error &&
		(error.code === "ENOENT" || error.code === "ENOTDIR")
	);
}
